# FastAPI and Server (REQUIRED - Install first)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6

# Database (REQUIRED)
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Authentication (REQUIRED)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Utilities (REQUIRED)
python-dotenv==1.0.0

# ML/AI (OPTIONAL - Can install later when needed)
# For now, we'll skip these to get the server running
# sentence-transformers==2.7.0
# scikit-learn>=1.3.0
# numpy>=1.24.0
