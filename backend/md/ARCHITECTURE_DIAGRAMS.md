# FastAPI Request Flow Diagram

## 📊 How a Request Flows Through Your App

```
┌─────────────────────────────────────────────────────────────┐
│  CLIENT (Frontend/Mobile App)                               │
│  Sends: POST /api/auth/login                                │
│         Body: {email: "user@example.com", password: "..."}  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  FASTAPI APP (main.py)                                      │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ 1. CORS Middleware ✅                                  │ │
│  │    - Checks origin                                     │ │
│  │    - Adds CORS headers                                 │ │
│  └─────────────────────┬─────────────────────────────────┘ │
│                        │                                     │
│  ┌─────────────────────▼─────────────────────────────────┐ │
│  │ 2. Router Match ✅                                     │ │
│  │    - Finds: auth.router                                │ │
│  │    - Endpoint: /api/auth/login                         │ │
│  └─────────────────────┬─────────────────────────────────┘ │
└────────────────────────┼─────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  ENDPOINT (api/v1/auth.py)                                  │
│  @router.post("/auth/login")                                │
│  def login(form_data, db: Session = Depends(get_db)):      │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ 3. Dependencies Execute FIRST ⚡                      │ │
│  │    ┌─────────────────────────────────────┐            │ │
│  │    │ get_db() from db/session.py         │            │ │
│  │    │ - Creates DB session                │            │ │
│  │    │ - Yields session to endpoint        │            │ │
│  │    │ - Will close after endpoint returns │            │ │
│  │    └─────────────────────────────────────┘            │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ 4. Endpoint Logic Runs 🎯                             │ │
│  │    - Query user by email                              │ │
│  │    - Verify password hash (security.py)               │ │
│  │    - Create JWT token (security.py)                   │ │
│  │    - Return token                                      │ │
│  └───────────────────────────────────────────────────────┘ │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  RESPONSE                                                   │
│  {                                                          │
│    "access_token": "eyJhbGc...",                           │
│    "token_type": "bearer",                                 │
│    "user": {...}                                           │
│  }                                                          │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  CLIENT                                                     │
│  - Stores token in localStorage                            │
│  - Uses for future requests                                │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Protected Endpoint Flow (with Authentication)

```
┌─────────────────────────────────────────────────────────────┐
│  CLIENT                                                     │
│  Sends: GET /api/match/jobs                                │
│         Headers: Authorization: Bearer eyJhbGc...          │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  FASTAPI APP                                                │
│  Routes to: match.router → /api/match/jobs                 │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  ENDPOINT (api/v1/match.py)                                 │
│  @router.get("/match/jobs")                                 │
│  def get_matched_jobs(                                      │
│      current_user: User = Depends(get_current_user),       │
│      db: Session = Depends(get_db)                          │
│  ):                                                         │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ Dependencies Execute in Order:                         │ │
│  │                                                        │ │
│  │ 1️⃣ get_db()                                           │ │
│  │    └─> Creates DB session                             │ │
│  │                                                        │ │
│  │ 2️⃣ oauth2_scheme (in get_current_user)               │ │
│  │    └─> Extracts: "Bearer eyJhbGc..."                  │ │
│  │    └─> Returns: "eyJhbGc..."                          │ │
│  │                                                        │ │
│  │ 3️⃣ get_current_user(db, token)                        │ │
│  │    └─> Decodes JWT                                    │ │
│  │    └─> Payload: {sub: 123, exp: ...}                 │ │
│  │    └─> Query: User.id == 123                          │ │
│  │    └─> Returns: User object                           │ │
│  │                                                        │ │
│  │ If ANY step fails → 401 Unauthorized                  │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ Endpoint Logic Runs with:                             │ │
│  │ - current_user: Full User object ✅                   │ │
│  │ - db: Active database session ✅                      │ │
│  │                                                        │ │
│  │ Now can safely:                                        │ │
│  │ - Access current_user.id                              │ │
│  │ - Query current_user's CV                             │ │
│  │ - Run matching algorithm                              │ │
│  │ - Return personalized results                         │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Database Session Lifecycle

```
Request Arrives
     │
     ▼
┌─────────────────┐
│  get_db() called│
│  ┌────────────┐ │
│  │ Create     │ │
│  │ Session    │ │
│  └─────┬──────┘ │
│        │        │
│        ▼        │
│  ┌────────────┐ │
│  │ yield db   │ │  ← Endpoint gets db here
│  └─────┬──────┘ │
└────────┼────────┘
         │
         ▼
┌─────────────────────────┐
│  Endpoint runs          │
│  - db.query(...)        │
│  - db.add(...)          │
│  - db.commit()          │
└─────────┬───────────────┘
          │
          ▼ (endpoint finishes)
┌─────────────────┐
│  get_db()       │
│  finally block  │
│  ┌────────────┐ │
│  │ db.close() │ │  ← Always runs, even on error
│  └────────────┘ │
└─────────────────┘
```

**Key Point**: Session ALWAYS closes, even if endpoint crashes!

---

## 🎯 Configuration Flow

```
Application Starts
     │
     ▼
┌──────────────────────────────────────┐
│  core/config.py                      │
│  ┌────────────────────────────────┐  │
│  │ Settings(BaseSettings)         │  │
│  │                                │  │
│  │ class Config:                  │  │
│  │   env_file = ".env"  ◄─────────┼──┼─ Reads .env file
│  │                                │  │
│  │ Reads environment variables:   │  │
│  │ - DATABASE_URL                 │  │
│  │ - SECRET_KEY                   │  │
│  │ - ACCESS_TOKEN_EXPIRE_MINUTES  │  │
│  │ - etc.                         │  │
│  └────────────────────────────────┘  │
│                                      │
│  settings = Settings()  ◄────────────┼─ Single instance
└──────────────┬───────────────────────┘
               │
               ▼
    Used everywhere via import:
    from app.core.config import settings
    
    Examples:
    - db/session.py uses settings.DATABASE_URL
    - security.py uses settings.SECRET_KEY
    - main.py uses settings.CORS_ORIGINS
```

---

## 🔐 JWT Token Structure

```
JWT Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOjEyMywiZXhwIjoxNjk...

Decodes to:
┌────────────────────────────────────┐
│  HEADER                            │
│  {                                 │
│    "alg": "HS256",                 │
│    "typ": "JWT"                    │
│  }                                 │
└────────────────────────────────────┘
┌────────────────────────────────────┐
│  PAYLOAD                           │
│  {                                 │
│    "sub": 123,        ← User ID    │
│    "exp": 1699999999  ← Expires    │
│  }                                 │
└────────────────────────────────────┘
┌────────────────────────────────────┐
│  SIGNATURE                         │
│  HMACSHA256(                       │
│    base64UrlEncode(header) + "." + │
│    base64UrlEncode(payload),       │
│    SECRET_KEY                      │
│  )                                 │
└────────────────────────────────────┘

Verification:
1. Decode header + payload
2. Re-compute signature with SECRET_KEY
3. Compare signatures
4. If match → Valid token ✅
5. Check expiration
6. Extract user_id from "sub"
```

---

## 📦 Module Import Flow

```
main.py imports:
├── from app.core.config import settings
│   └── config.py imports:
│       └── from pydantic_settings import BaseSettings
│
├── from app.api.v1 import auth, jobs, match, cv
│   └── auth.py imports:
│       ├── from app.db.session import get_db
│       ├── from app.core.security import verify_password, create_access_token
│       └── from app.models.user import User
│
└── app.include_router(auth.router)
    └── Registers all endpoints from auth.py
```

**Key Points**:
- No circular imports (models don't import routers)
- Clear hierarchy (api → services → models)
- Dependencies flow downward

---

## 🎓 Dependency Injection Deep Dive

```python
# Without Dependency Injection (OLD WAY)
@router.get("/match/jobs")
def get_matched_jobs():
    # Manual work:
    token = extract_token_from_header()
    if not token:
        raise HTTPException(401)
    
    user_id = decode_token(token)
    if not user_id:
        raise HTTPException(401)
    
    db = SessionLocal()
    try:
        user = db.query(User).get(user_id)
        if not user:
            raise HTTPException(404)
        
        # Finally do the actual work
        matches = find_matches(user, db)
        return matches
    finally:
        db.close()

# With Dependency Injection (NEW WAY)
@router.get("/match/jobs")
def get_matched_jobs(
    current_user: User = Depends(get_current_user),
    db: Session = Depends(get_db)
):
    # Just do the work! Everything else handled:
    matches = find_matches(current_user, db)
    return matches
```

**Benefits**:
- ✅ 90% less code
- ✅ Automatic error handling
- ✅ Automatic cleanup
- ✅ Easy to test (mock dependencies)
- ✅ Reusable across endpoints

---

## 🚀 Summary

Your backend uses:
1. **Router system** → Organizes endpoints by feature
2. **Dependency injection** → Injects what you need automatically
3. **Configuration** → Centralized, type-safe settings
4. **Security layer** → JWT + password hashing
5. **Session management** → Automatic DB connection handling

**Everything flows cleanly from request → response! 🎯**
